name: Deploy Backend (FTP / runasp.net)

on:
  push:
    branches: [master]
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"
  pull_request:
    branches: [master]
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:

concurrency:
  group: deploy-backend-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  DOTNET_VERSION: "10.0.x"

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore Pharmacy.sln

      - name: Build
        run: dotnet build Pharmacy.sln -c Release --no-restore

      - name: Test
        run: dotnet test Pharmacy.sln -c Release --no-build --verbosity normal

      - name: Publish API for IIS
        run: |
          dotnet publish src/Pharmacy.Api/Pharmacy.Api.csproj \
            -c Release \
            -o "$GITHUB_WORKSPACE/artifacts/backend" \
            --no-build

      - name: Ensure logs folder for IIS stdout
        run: mkdir -p "$GITHUB_WORKSPACE/artifacts/backend/logs"

      - name: Upload publish artifact
        uses: actions/upload-artifact@v7
        with:
          name: pharmacy-api
          path: artifacts/backend
          if-no-files-found: error

  deploy-ftp:
    name: FTP Deploy (runasp.net)
    needs: build-test
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment: production-backend

    steps:
      - name: Validate deploy secrets
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          missing=0
          for var in FTP_SERVER FTP_USERNAME FTP_PASSWORD; do
            if [ -z "${!var}" ]; then
              echo "::error::Secret '${var}' is not set. Add it in Settings → Secrets and variables → Actions, or under the 'production-backend' environment."
              missing=1
            fi
          done
          exit $missing

      - name: Checkout
        uses: actions/checkout@v7

      - name: Download publish artifact
        uses: actions/download-artifact@v8
        with:
          name: pharmacy-api
          path: deploy

      - name: Write production settings overlay
        env:
          DB: ${{ secrets.DATABASE_CONNECTION_STRING }}
          JWT: ${{ secrets.JWT_KEY }}
        run: python3 .github/scripts/write-backend-production-settings.py

      - name: Deploy via FTP to runasp.net (/api)
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          local-dir: ./deploy/
          # Same FTP host as frontend; API lives in /api subfolder
          server-dir: ${{ vars.FTP_API_DIR || './api/' }}
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/logs/**
